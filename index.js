/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './src/application/App';
import { name as appName } from './src/application/app.json';

AppRegistry.registerComponent(appName, () => App);
